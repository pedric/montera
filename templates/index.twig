{% extends "base.twig" %}

{% block content %}

{% include 'carousel.twig' %}

{% include 'services-tease.twig' %}

	<h2>{{ foo }}</h2>
	<p>{{ qux }}</p>
	{% for post in posts %}
		{% include ['tease-'~post.post_type~'.twig', 'tease.twig'] %}
	{% endfor %}
{% endblock %}
